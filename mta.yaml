_schema-version: "3.2"
ID: devfacturascopia
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: devfacturascopia-destination-content
  type: com.sap.application.content
  requires:
  - name: devfacturascopia-destination-service
    parameters:
      content-target: true
  - name: devfacturascopia-repo-host
    parameters:
      service-key:
        name: devfacturascopia-repo-host-key
  - name: devfacturascopia-uaa
    parameters:
      service-key:
        name: devfacturascopia-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: devfacturascopia_html_repo_host
          ServiceInstanceName: devfacturascopia-html5-srv
          ServiceKeyName: devfacturascopia-repo-host-key
          sap.cloud.service: devfacturascopia
        - Authentication: OAuth2UserTokenExchange
          Name: devfacturascopia_uaa
          ServiceInstanceName: devfacturascopia-xsuaa-srv
          ServiceKeyName: devfacturascopia-uaa-key
          sap.cloud.service: devfacturascopia
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: devfacturascopia-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: devfacturascopia-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - devfacturascopia.zip
      name: devfacturascopia
      target-path: resources/
- name: devfacturascopia
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: devfacturascopia-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: devfacturascopia-destination-service
    service-plan: lite
- name: devfacturascopia-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: devfacturascopia-xsuaa-srv
    service-plan: application
- name: devfacturascopia-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: devfacturascopia-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
