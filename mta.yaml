_schema-version: "3.2"
ID: devfacturas
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: devfacturas-destination-content
  type: com.sap.application.content
  requires:
  - name: devfacturas-destination-service
    parameters:
      content-target: true
  - name: devfacturas-repo-host
    parameters:
      service-key:
        name: devfacturas-repo-host-key
  - name: devfacturas-uaa
    parameters:
      service-key:
        name: devfacturas-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: devfacturas_html_repo_host
          ServiceInstanceName: devfacturas-html5-srv
          ServiceKeyName: devfacturas-repo-host-key
          sap.cloud.service: devfacturas
        - Authentication: OAuth2UserTokenExchange
          Name: devfacturas_uaa
          ServiceInstanceName: devfacturas-xsuaa-srv
          ServiceKeyName: devfacturas-uaa-key
          sap.cloud.service: devfacturas
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: devfacturas-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: devfacturas-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - devfacturas.zip
      name: devfacturas
      target-path: resources/
- name: devfacturas
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: devfacturas-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: devfacturas-destination-service
    service-plan: lite
- name: devfacturas-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: devfacturas-xsuaa-srv
    service-plan: application
- name: devfacturas-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: devfacturas-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
